<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.1"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">

    <persistence-unit name="MercatoPU" transaction-type="JTA">

        <!-- JTA DataSource registered in server.xml -->
        <jta-data-source>jdbc/MercatoDB</jta-data-source>

        <class>it.paskinomercato.model.Categoria</class>
        <class>it.paskinomercato.model.Prodotto</class>
        <class>it.paskinomercato.model.Cliente</class>
        <class>it.paskinomercato.model.Indirizzo</class>
        <class>it.paskinomercato.model.Ordine</class>
        <class>it.paskinomercato.model.RigaOrdine</class>

        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <properties>
            <!-- EclipseLink (Liberty's default JPA provider) -->
            <property name="eclipselink.target-database"     value="PostgreSQL"/>
            <property name="eclipselink.logging.level"       value="WARNING"/>
            <!-- Never generate/alter the schema — the DDL is managed via schema.sql -->
            <property name="javax.persistence.schema-generation.database.action" value="none"/>
        </properties>

    </persistence-unit>

</persistence>
